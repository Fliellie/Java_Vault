## So Sanh double
```
Double.compare(other.price, price) == 0
```
## So sánh hai nội dung String
```
String1.equals(String2);
// lưu ý : phương thức này được kế thừa từ class object, vậy nên equals có trong rất nhiều các lớp con, và ta cũng có thể chỉnh sửa nó
public boolean equals(Object obj){}
// trong class gốc, nó chỉ so sánh xem 2 biến có cùng địa chỉ hay không
```